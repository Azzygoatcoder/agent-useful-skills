---
name: self-evolve
description: 科研骨架自进化闭环——读各 skill 的自进化日志，把累积教训晋升为核心规则。Use when 用户说"自进化"、"skill 进化"、"晋升教训"、"consolidate 日志"、"日志该不该进核心流程"、"复盘 skill"。
---

# Self-Evolve — 自进化闭环

把「自进化日志」从被动记录变成主动闭环：定期读日志 → 判断哪些教训该晋升为核心流程 → 落地改 SKILL.md → 记「已晋升」。

## 场景判定

| 场景 | 触发 | 动作 |
|------|------|------|
| A. 汇总 | "consolidate"/"看看所有日志" | grep 所有 skill 日志 → 汇总视图 |
| B. 单 skill 进化 | "进化 paper-reading"/"X 的日志该晋升了吗" | 读该 skill 核心流程 + 日志 → 提晋升建议 |
| C. 顺带检查 | 改完一个 skill 后 | 记日志一行；同类教训重复 ≥2 次 → 提示晋升 |

## 场景 B：单 skill 进化（核心）

1. **读**：该 skill 的 `## 自进化日志` 表 + 核心流程（场景判定表 / 工作流步骤）
2. **判**：逐条日志分三类——
   - **晋升**：教训已稳定复用（≥2 次或实战验证）但核心流程还没写 → 写进 SKILL.md 主体
   - **过时**：核心流程已更新、此条已被吸收 → 从日志删或标记
   - **保留**：仍是一次性经验 → 留日志
3. **提**：产出「晋升/过时」的具体改法（贴 SKILL.md 片段 diff，不整篇重写）
4. **改**：人工确认后走 writing-skills 流程落地 → 验证
5. **记**：日志加一行 `已晋升 | 日期 | 把 X 写进核心流程`

## 晋升判据

- 同一条教训在日志出现 ≥2 次，或实战中反复触发（"又踩了这个坑"），或指向核心流程空白
- **不晋升**：单次偶然、与核心流程无关的一次性技巧

## 工具

| 工具 | 用途 |
|------|------|
| Grep `## 自进化日志` | 定位所有 skill 的日志 |
| writing-skills skill | 落地改 SKILL.md |
| review.py（可选） | 跨模型复核「晋升建议是否过度理论化」 |

## 自进化日志

| 日期 | 学习来源 | 吸收的模式 |
|------|---------|-----------|
| 2026-08-13 | 首版 | 自进化闭环 v1：汇总/进化/顺带三场景；晋升判据=复用 ≥2 次或核心流程空白 |
