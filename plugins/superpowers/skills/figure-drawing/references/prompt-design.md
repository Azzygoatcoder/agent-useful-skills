## gpt-image-2 的地位（2026-08-03，控制变量实测修正）

**gpt-image-2 只对【示意/概念/封面图】有价值**；**结构化架构/流程图直接画矢量更好**（控制变量实验：同一 LLSM 素材、同英文，第三方盲评直绘 9.5 vs gpt-image-2 路线 7——布局 9vs6、层级 9vs7、配色 8vs7）。

**为什么直绘赢**：直绘天生为矢量设计（对齐/间距/无交叉内化）；gpt-image-2 复刻继承光栅布局（手绘感/方形构图）→ 间距不均、箭头交叉、缺图例。

**选路规则（修正）**：
- **结构化图**（架构/流程/pipeline）→ **直接画矢量**（deepseek，控制变量实测更好）
- **示意/概念/封面**（需要视觉冲击、deepseek 画不出的设计感）→ **gpt-image-2** 出设计再复刻
- 之前的"gpt-image-2 是设计提供者"结论是**单图孤立评估的偏差**，控制变量纠正了
## gpt-image-2 设计语法（从 3 张 gpt-image-2 产出的参考图提炼，2026-08-03）

> 三张参考图（EDA方法/TurnOPD/ChipMATE）**全部是 gpt-image-2 画的**——这套"设计共性"实为 gpt-image-2 的设计语法，正是我们要借鉴的。目标：把语法教给 deepseek，让它直绘时自带 gpt-image-2 的设计感（直绘赢在布局，语法补设计丰富度）。

| 共性 | 落地 |
|------|------|
| **叙事逻辑** | 图要讲故事："问题→方法/机制→验证/效果"。生成 prompt 时先定叙事线，再铺元素 |
| **多元素整合** | 一张图可混合：架构/流程 + 数据可视化（曲线/柱状）+ 概念/机制框。按需组合 |
| **主题色+2-3辅助色** | 一个主色贯穿全图（如紫 #7B2CBF），2-3 个辅助色对比（蓝青/绿），不用彩虹 |
| **视觉层级** | 标题/主模块最突出，数据强调，次要标注弱化 |
| **图例清晰** | 颜色/线型语义在图例里说清 |

**独特性（不可模板化，留给 prompt）**：领域决定元素组合（EDA→流程、LLM→数据曲线、系统→贡献/愿景）、叙事深度（3-6 段）、是否需要机制解释框。

## 生图 Prompt 工程（写规格，不写描述）

从"P 图思维"转向"**写规格思维**"——把布局/颜色/箭头/字号全定义清楚，别堆"漂亮/高清/专业"这类形容词（那样只会得到看似不错但不达标的图）。

**六大要素**（增强 PDCF）：图的类型 + 主体对象 + 机制链条 + 构图方式 + 风格约束 + 负面限制。

**几何形态语义映射**（认知心理学依据，别用方块代替一切）：
| 实体 | 形态 |
|------|------|
| 数据/流动 | 平行四边形/圆柱 + 方向箭头 |
| 算法/过程 | 直角矩形（编解码用漏斗/阶梯） |
| 决策 | 菱形 + 互斥分支 |
| 结论/终态 | 圆角矩形/椭圆 |
| 抽象概念（神经网络等） | 具象化特征简图 |

**配色规则**：
- **三色域**：全图主色调编码 ≤3 组
- **80/20**：八成低饱和冷灰/米白，关键路径/结论用高饱和强调色
- 通行惯例：白底 + 蓝青主色 + 橙红强调关键事件；给 hex 精确控制（如浅蓝 #B3F2FD / 深蓝 #1976D2）

**文本规范**：全图两种字号；专有名词首字母大写（禁全大写）；图内禁长文/括号备注。

**常见误区**：颜色多≠好（顶刊克制）；信息多≠好（解释性文字放正文）；渐变/阴影≠高级（学术高级感来自克制/留白/逻辑清晰）。

**强化句**：生成时加 `Do not simplify, follow every detail exactly.`；多轮迭代**每轮只改一个地方**。

## 提上限模式（场景 B 顶配，2026-08-03）

**触发**：场景 B（对外发表）里的顶配需求——graphical abstract、封面、审稿前关键图。理解深度 L3-L4（深读原文），把 gpt-image-2 的设计上限用足。理解深度决定 prompt 上限：L1定位/L2结构(普通图)/L3机制(叙事图)/L4设计洞察(顶配)。

**5 阶段**：深读原文 → 提炼洞察 → 写 prompt → 生成 → 验证

**① 深读原文**：定位（arXiv/DOI）→ 读方法+图表+结论（结合阅读报告补 L2→L3）→ 提创新点/流程/关键数字。

**② 提炼洞察（6 问框架，人参与定稿）**：
1. 本质：一句话本质（区别于同类的核心）
2. 信息：图只传达一个信息，是什么？
3. **隐喻：这个工作像什么？（视觉类比——上限的关键）**
4. 叙事：从哪到哪？（输入→机制→输出）
5. 差异化：和 baseline 比创新在哪？图里体现吗
6. 突出：哪个模块值得视觉强调

> **agent 给 6 问草稿 → 用户确认/修正隐喻**（视觉隐喻是设计判断，人把关上限；AI 提供候选，人的品味定稿）

**③ 写 prompt（三部分，提上限版）**：
- **[设计意图]** 本质+隐喻+风格（顶会审美，Nature/Science 级）
- **[严格结构]** L3 机制，实体/流程/关系精确陈述（正确性优先）
- **[设计系统方向]** 构图焦点/配色和声+hex/视觉手法（深度/层次/流动感）——给方向不规定死，让 gpt-image-2 发挥
- **[负面]** 禁杂乱/劣质渐变/廉价3D/通用库存图感

**④ 生成 → ⑤ 验证**：`generate_image` → `vision.py` 查结构/文字是否对得上理解、质量是否顶配；迭代每轮只改一个维度。
